/* Gramática para expresiones aritméticas básicas */
/* Tokens del lenguaje */
%token PLUS TIMES LPAREN RPAREN NUMBER WS
IGNORE WS

%%

/* Producciones de la gramática */
expr:
    expr PLUS term
  | term
;

term:
    term TIMES factor
  | factor
;

factor:
    LPAREN expr RPAREN
  | NUMBER
;
